# Cockpit BACKLOG — 唯一权威待办登记册
> 规则：任何 caveat / 待验证 / 待完成，一律登记在此（不再只活在对话里）。宣布"完成/正确"前必须：
> ① 跑 `python3 selfcheck.py`（须 PASS）；② 把真实输出（邮件）对照 **live IBKR 连接器** 逐项核对；
> ③ 确认 config 键全被引用、文档与代码一致。最后更新本表状态。

更新：2026-08-22（会话续接·全量现实核对：GitHub↔本地 21 文件逐字节一致；日报 07-20→08-21 连续、唯一失败=08-06 GHA 取消→B43；B29/B31/B32/B33/B34/B37/B38/B39/B42 凭 08-21 日报+08-22 双周实弹邮件关闭；B24 修复已交付；新增 B43、D2；SKHY/RAM 身份已 FMP 复核；D1 补记已定；同晚：红线v2杠杆ETF窄门批准→D2关、B35提醒won't-do、B40确认存活、8文件粘贴经 git diff 核验一致；深夜 Phase A：B44 再入场/B45 止盈阶梯/B46 现金流向/B36 主题敞口 四项代码交付并粘贴核验一致；Phase C：B28+B41 全市场扫描器 `scanner.py` 交付；08-23 Phase B：十层研究 v1 + 处置手册交付，config 新增 5 个子板块，B35 彻底关闭；用户指出「手册应该是系统能力而非一次性交付」→ 登记 B48 持仓体检表并当日建成；工作区新增 00_README_索引.md 定死文档生命周期规则；反应式排查登记 B49-B52+D3；**交付路径改革：先建 B52 CI 闸门，再把本地 cockpit/ 改为真 git clone，终结手工粘贴**）

## R — 专业化瘦身与决策统一（用户 2026-08-28 立项；本轮唯一主线）

范围：Cockpit 只管 IBKR 账户的投研/仓位/风控/交易建议。**明确排除**：嘉信 QQQ 定投、税务、自动下单。持有周期 1-6 个月或更长；25-35% 年化为进取目标（非承诺）；40% 为绝对最大回撤红线。本轮只做 P0：简化结构、统一决策、修正确定性错误。**不加新扫描器、新指标、新邮件章节、新 B 编号补丁。**

用户当轮拍板（2026-08-28）：
- **单票硬顶固定 $30,000**，定性为「基于全体财富风险承受能力的绝对上限」；嘉信不由本系统管理，不进入该数字。
- **组合热度 6-8% = 警示层，不参与裁决**。绑定上限只有：硬退出 / $30k 单票硬顶 / 40% 主题集中度 / 账户安全（红线 v2 杠杆窄门）。vol×corr 与风险档反推降为参考口径，只解释、不产生第二个答案。
- **40% 回撤红线本轮只记录，不建规则**（需先有含现金流的绩效账本才算得准）。
- **止损口径本轮不动**（保持 20 日低×0.99）；是否随 1-6 月持有期放宽，另立项、用 B51 回放框架做参数对比后再定。

| id | 项 | 状态 |
|----|----|------|
| R1 | **黄金回归测试**：`tests/`（stdlib unittest，无 pytest 依赖）。基线取自 IBKR 对账单 U22209151（2025-11-03..2026-08-27）——净值 159,528.31 / 现金 −9,186.07 / 股票 168,877.45 / 应计 −171.31+8.24 / TWR −1.62% / 入金 160,000 / 12 个持仓的股数·成本·市值·已实现盈亏。七条失败测试**成对写**：先证明检查器能抓住 2026-08-28 那封真实邮件里的缺陷，再指向系统本身。 | ✅ 已建 2026-08-28 — 44 个测试，39 绿 / 5 红（红为设计红，见 R3-R7）；`selfcheck` 新增闸门 11 把它纳入 CI，并已用三种反向破坏验证闸门会响 |
| R2 | **唯一 Decision 对象与裁决器**：`cockpit/domain/models.py`（RuleProposal / Decision）、`cockpit/engine/resolve.py`、`cockpit/rules/{account,thesis,concentration,sizing,exit,profit}.py`。规则只能产出 RuleProposal，渲染层只能读 Decision，**禁止重算金额**（闸门 12 机械拦截）。 | ✅ 已建 2026-08-28 — 2026-08-27 账面解析为 **3 条卖出、总额 $31,842**（旧邮件同一账面给出 72,858 / 86,442 / 109,543 三个数）；memory_hbm 削减后正好 40.0%；现金 −9,186 → +22,656。冻结在 `tests/test_resolution_20260827.py` |
| R3 | **Flex `as_of` 必须驱动记账日期**：`_append_nav(today, ...)` 用的是运行日。2026-08-27 的持仓与净值被记在 2026-08-28 名下，`nav_history` 里 08-27 根本没有条目。 | 🔴 **OPEN（代码已交付 2026-08-28，实盘未验证）** — `cockpit/ledger/performance.append_nav_at_as_of()`；`daily_brief` 的 nav 与 signal_history 两处写入均已改为按 as_of 记账，并给旧条目打上 `_legacy_keys_are_run_dates_through` 标记。**历史序列没有重写**：只有 2026-08-28 那条经对账单核实（159,528.31 = 08-27 净值），其余日子无法逐日核实，把看得见的错误换成看不见的错误不划算。验收=下一封真实日报里 nav_history 出现 as_of 键 |
| R4 | **绩效账本必须含实际成交、外部现金流、费用与利息，并输出 TWR**。三条独立缺陷，已按 `state/signal_history.json` 实数据逐条核实（**我最初的描述说过头了，这是更正后的版本**）：① `选股/尺寸` 两桶里 `if not vN: continue` 让**窗口内离场的标的贡献恒为 0**——NVDA（最后出现 08-26，08-27 卖出，已实现 +$1,444.67，全期最大赢家）、AVGO、SPCX（+$1,120.45）全部不参与。② `市场 P&L` 桶不是全零，而是**最后一段被丢掉**：`if not vb: continue` 跳过标的消失的那个日对，于是 NVDA 08-27 财报后 +8.7% 那一跳和实际卖出全部不可见。③ 被丢掉的那部分并没有消失，它落进 `residual`，而 `residual` 在代码与邮件里被标注为「入金/利息/费用」——**离场标的的盈亏被误记成了外部现金流**。另：`_performance()` 自己标注「not deposit-adjusted」，而全期入金 $160,000，那个百分比无意义（对账单 TWR = **−1.62%**，MTM −11,003.36，账户实际是亏的）。 | 🔴 **OPEN（代码已交付 2026-08-28，实盘未验证）** — `cockpit/ledger/performance.py`：`attribution_with_exits()` 把离场标的按 `broker / estimated / backfill / unknown` 四种定价依据分别列出，**无价可用时报告「未知」而不是 0**；`twr()` 日度链接并中和现金流。`state/cash_flows.json` 由对账单播种（100,000 @ 05-28 + 60,000 @ 06-29，六条明细逐条核实过，净额 = 对账单的 160,000）。`biweekly_review._attribution()` 已改为调用账本，旧实现保留为 `_attribution_legacy` 仅供回归测试证明缺陷真实存在。**TWR 的拒绝逻辑改过一次**：初版「窗口超出对账日期就拒绝」= 永久拒绝 = 安全但无用，现改为「照常给数 + 点名未对账的尾巴」，只在扫描到「单日净值跳变 ≥15% 且无现金流记录」这种**有证据**的情况下才拒绝并指出具体日期。 |
| R5 | **`theme_overrides` 必须进入相关性/同行扩展/主题敞口/层内排名**：`risk.position_caps()` 收到的是 `config.subthemes` 单独构建的映射，**7 个 override 标的全部不可见**——SKHY（31% 净值）与 MU（24.5% 净值）同属 memory_hbm，却拿不到 ≥0.60 同主题相关性下限，即全书最集中的一对被当成互不相关。 | 🔴 **OPEN（代码已交付 2026-08-28，实盘未验证）** — `_theme_of()` 现在把 `theme_overrides` 叠加进去，`daily_brief` 与 `biweekly_review` 共用同一份映射（后者 import 前者）。**副作用要注意**：SKHY/MU 现在会吃到同主题相关性下限，vol×corr 上限会收紧——但该口径已降为参考层，不改变卖出总额。验收=下封日报 vol×corr 数字变化且卖出总额不变 |
| R6 | **IBKR 邮件移除嘉信内容**：`_MKT_ZONE_CN` 三条文案硬编码了另一个账户的月度定投指引。 | 🔴 **OPEN（代码已交付 2026-08-28，实盘未验证）** — 三条文案改为只讲本账户的开仓约束；市场位置评分保留（它闸门 IBKR 买入）。**原文案不复制进注释**——注释里引用被删的字符串会让源码级守卫失效，我第一版就踩了这个（与闸门 10 同一类错误）。验收=下封日报首屏无该文案 |
| R7 | **残仓噪声闸门写进 config**：`risk.min_decision_pct_nav: 0.25` / `risk.min_decision_usd: 200`（另补 `leveraged_etf_max_pct_nav: 5`）。 | ✅ DONE 2026-08-28 — config 与裁决器默认值由测试强制一致（政策写在 config 而默认值藏在代码里 = 两套政策）。MRVL 0.741 股／$179 仓位上的 $59 止盈已被拦下并留下可读理由 |
| R8 | 拆分 `daily_brief.py` 为 rules / engine / render / ledger；`daily_brief` 只留 读数据 → 调规则 → 调裁决 → 渲染 → 投递成功后提交状态。 | 🔴 **OPEN，且本轮方向是反的**：删掉 `_action_plan`(97 行) + `_dispose_order`(40 行) 之后，又加了 `_build_decisions` / `_decision_inputs` / `_staleness` / `_followups_md` / `_cash_routing_md`，**净结果 882 → 968 行**。新逻辑确实进了 `rules/engine/render/ledger` 五个子包（合计约 1,050 行），但 `daily_brief` 自己没瘦，只是换了内容。**下一轮必须把 `_holdings_snapshot` / `_position_audit` / `_exceptions` / `_snapshot_md` / `_candidates_md` 真正搬出去，并盯着行数验收，不能只看「有没有建子包」。** |
| R9 | config 与文档瘦身：`config.yaml` 只留 IBKR 范围的稳定政策与运行参数；公司身份/主题/核验日期迁到 `research/holdings.yaml`；事故复盘迁到 `docs/incidents/`；已完成功能迁到 `CHANGELOG.md`；BACKLOG 只留未完成。保留一个迁移周期的旧键兼容与弃用提示。 | 🔴 OPEN |
| R10 | **新版行动清单格式**：首屏只给 账户时间戳／净值·现金·杠杆·组合在险／今天是否允许买入／最终卖出总额／按优先级排列的决策。每条含 标的·方向·现股数→目标股数·需买卖股数·目标金额·绑定规则·核心理由·有效期·失效条件·数据时间。 | 🔴 OPEN |
| R11 | **验收：用真实 8/28 数据重生成一封邮件并与 IBKR 对账单逐项比较**。**代码写完不等于关闭**——实盘邮件未验证的项目一律保持 OPEN。 | 🔴 OPEN |

### R 追加约束（用户 2026-08-28 补充，来源：项目 Claude Project memory 复查）

| id | 项 | 状态 |
|----|----|------|
| R12 | **B60 强制回归测试**：会消耗一次性内容的状态（`last_positions`、`reentry.prompted`、reflection memory）只能在 `notify.send() == True` 之后提交；模拟发送失败必须全部保持未消费。 | ✅ DONE 2026-08-28 — `tests/test_b60_deferred_writes.py`（11 个测试）驱动**真函数 + 真临时 state 文件**：检测到离场→不落盘→下一次运行仍能重新检测；flush 后恰好消费一次；`main()` 在 send 失败时 `exit 1` 且 flush 次数 = 0。**两次生产级反向破坏均正确触发**：把 `_defer` 改成立即执行 → 5 个测试红；让 `main()` 在失败时也 flush → 3 个测试红。另含一条专门的测试证明「源码里有 `flush_pending_writes()` 字样」在行为被破坏时**仍然是绿的**——即字符串检查不足以替代行为测试 |
| R13 | **所有报告与 Decision 使用 Flex 实际 `as_of`；滞后必须明写，不得伪装成实时结论；FMP EOD 不含盘后须在价格敏感场景降级。** | ✅ DONE 2026-08-28 — `Decision.as_of` 取 Flex 期末日；`_staleness()` 产出三类标注并渲染进首屏「数据新鲜度」行：① 滞后 N 日 + 「不是实时的」；② 「价格为 FMP 收盘价，不含盘后与隔夜」恒常显示；③ **在数据时点之后发布财报的标的逐个点名并「降级为参考」**（2026-08-27 的 MRVL 正是此例）。🔴 实盘邮件未验证 |
| R14 | **Intraday Alert 保持停用**；其 B48 之前的 `max(200DMA, 成本×0.8)` 止损口径不得进入统一裁决器；是否彻底删除旧 workflow/config 另列，不在重构中偷改运行行为。 | ✅ DONE（本轮的部分）2026-08-28 — 闸门 14：`domain/engine/rules/render/ledger` 五个目录任一 import `intraday_alert` 或出现 `cost×0.8` 即 FAIL；反向破坏两种均触发。`intraday-alert.yml` 与 `config.alerts.*` **本轮一字未动**。🔴 **R14b OPEN（另列）**：是否删除 yml 的 cron 与 `alerts.intraday_cron_utc`——用户 2026-08-28「可以清，但先别急」 |
| R15 | **能驱动买卖/主题分类/清仓顺序的外部事实必须带来源、核实日期、过期状态；未核实或过期只能形成警告。** | ✅ DONE 2026-08-28 — `cockpit/rules/facts.py`：解析「核实 YYYY-MM-DD，源=…」，另兼容配置里已有的两种写法（AAOI 的「核实**于**」、SKHY 的「核实 日期**：**来源」）；`risk.fact_expiry_days: 180` 超期即 `expired`。`thesis.propose()` 只有 `verified` 才能出 `HARD_EXIT`，`unverified`/`expired` 一律降为 REVIEW/FLAG。**过程更正**：我第一版解析器过严，把 AAOI 与 SKHY 判成未核实——SKHY 是 31% 净值的第一大仓，那会是一条凭空生成的假警告。是解析器错了，不是数据错了 |
| R16 | **安全边界在重构中不得丢失**：不自动交易/转账/撤单；Schwab 与税务继续排除；IBKR mascot 的 exclude 与杠杆 ETF 限制必须存活。 | ✅ DONE 2026-08-28 — 闸门 15 + `tests/test_safety_and_freshness.py`：下单/转账调用出现即 FAIL；`exclude` 丢失 IBKR、`leveraged_etf_max_pct_nav`/`leverage_factors`/`fact_expiry_days` 缺失、引擎里出现 Schwab/税务字样，全部 FAIL。五种反向破坏逐个验证过会响 |
| R17 | **交付纪律（用户 2026-08-28 补充）**：编辑前先看行尾与工作区状态；只 `git add` 明确路径；**不用 `git add -A`、不执行 `git checkout -- .`、不 `git reset --hard`、不碰 `_to_delete/`、不自动 push**。 | ✅ 已生效 — **我上一批交付里给出的 `git checkout -- .` 指令已作废并当面更正**；替代方案 `git pull --rebase --autostash`（stash 而非丢弃）。`_to_delete/` 本轮未触碰（内含唯一文件是我上一轮从 `.git/` 挪出的 index.lock） |
| R18 | **完成定义（重申）**：`selfcheck.py` 通过 + 反向故障测试通过 + **生成一封真实日报并与同期 IBKR 数据逐项核对**。生成出 HTML/PDF 或「代码路径存在」都不算完成。 | 🔴 OPEN — 离线重建已做（`tools/r11_rebuild_20260827.py`，25 项对账全部 0.0000 差异），但**真实邮件未发出、未核对**，因此不关闭 |

### R 政策一致性修正（用户 2026-08-28 第三批指示；提交前必须完成）

| id | 项 | 状态 |
|----|----|------|
| R19 | **单票硬顶唯一权威值**：新增 `risk.single_name_hard_cap_usd: 30000`。`account.total_assets_usd` 与 `risk.single_name_hard_cap_pct_of_total` 不再决定硬顶，仅保留一个迁移周期作回退并发弃用提示。 | ✅ DONE 2026-08-28 — 唯一解析器 `cockpit/domain/policy.hard_cap_usd()`；`daily_brief`、`biweekly_review`、`concentration` 三处旧算式全部替换；候选仓位的 `maxpos_pct` 亦由它推导。闸门 16：硬顶解析结果 ≠ $30,000 → FAIL；`policy.py` 以外任何模块自行推导 → FAIL。测试覆盖 `total_assets_usd` 取 0/10万/25万/40万/100万 与 pct 取 5/12/30，硬顶恒为 $30,000 |
| R20 | **杠杆 ETF 硬止损补齐**：`max(成本×0.85, 20日低×0.99)`，下跌中先触及者触发 `HARD_EXIT`（tier 1，压过一切尺寸规则）。`RULE_LEV_STOP` 不再是未使用常量。 | ✅ DONE 2026-08-28 — `account._leveraged_stop_level()` + `RULE_LEV_STOP`；新增 `risk.leveraged_etf_hard_stop_pct: 15`。**下跌中「先触及」= 两者中较高者**，故取 max。数据缺口（无成本价且无 20 日低）产出 FLAG 并明写「数据缺口，不是没有止损」，绝不静默放行。闸门 17 挡住「常量声明了但从不发射」 |
| R21 | **热度语义统一**：6-8% 只作「暂停新增风险」的买入闸门，不产生卖出金额，且文案不得自相矛盾。 | ✅ DONE 2026-08-28 — 旧文案在 ⛔ 旁边写「这是警示，不是禁令」，读者无法判断信哪一半。新文案：「→ **暂停新增风险**：今天不开新仓、不加仓。**它不要求你为了把在险打回 6-8% 而机械卖出**——卖出金额只由绑定规则决定（本次合计 $X）」。闸门 18 挡矛盾措辞 |
| R22 | **主题超限分配 = 方案 (a)**（用户 2026-08-28 拍板）：① 固定 $30,000 单票硬顶先行；② 重算杠杆调整后敞口；③ 仍超 40% 则**优先减杠杆产品**（每卖 $1 按 `leverage_factor` 抵减敞口）；④ 杠杆减到 0 仍超限，则在普通成员间**按主题敞口占比同比例**削减。**RS 只作观察，不决定绑定金额、分配顺序或清仓。** 本规则不覆盖账户安全 / 已核实结构性否决 / 杠杆硬止损 / 单票硬顶。 | ✅ DONE 2026-08-28 — `concentration.propose()` 重写；**`rs_by_ticker` 参数已删除而非留空**（留一个不用的参数等于给 RS 留后门）。闸门 19 拦截 RS 回流与分配方案静默变更，两种反向破坏均触发。测试 `tests/test_theme_allocation.py`（15 个）覆盖：冻结 08-27 结果、RS 翻转不改金额、杠杆减到 0 后按比例分配（含不等额成员按 4:1 比例验证）、未超限不产生卖出、恰好等于上限不产生卖出、硬止损与主题分配同时触发时硬止损优先且每票仍只有一个 Decision |

### R23 组合级顺序裁决（用户 2026-08-28 用 Codex 在本地真函数上复现的跨标的过度减仓缺陷）

**缺陷**：各规则都基于**原始 positions** 独立产出 Proposal，引擎只按单票取最严格值。「每票一个 Decision」把冲突压平了，却完全没有实现「按优先级逐层更新组合」——后面的主题规则看不到前面其他标的已经发生的退出。

**我复现时的一处更正**：用户描述的是 ORD 触发 tier 2 已核实 `HARD_EXIT`。但用当前真实代码，tier 2 硬退出只在 `outside_framework` 层产生，而那会把标的移出主题，该具体路径走不通。**缺陷本身完全成立**，我用 tier 5 破位硬止损（标的留在主题内）复现出了同样结果：NAV 100,000 / 主题上限 60% / ORD 60,000 / LEV 10,000@2x=20,000 敞口 → 旧引擎卖 **$70,000**、主题敞口归 0；正确答案是 ORD 退出后主题只剩 20,000 已达标，**只该卖 $60,000**。同一缺陷经 tier 1 杠杆硬止损同样可达。

| id | 项 | 状态 |
|----|----|------|
| R23 | **`cockpit/engine/pipeline.py`：组合级顺序裁决。** PASS A 先把**所有层的无条件硬退出**在原始账面上收齐并落到 working book（硬退出只依赖价格/成本/已核实事实，从不依赖仓位大小，所以在任何尺寸计算之前就是可知的，也**必须**先知道）；PASS B 按 tier 顺序逐层跑上限规则，**每层都在上层留下的 working book 上重新求值**；PASS C 在最终账面处理止盈与买入，且**已被更高层减过仓的标的不再二次止盈**。`resolve.py` 拆出共享的 `build_decisions()`，`resolve_decisions()` 降级为「单账面扁平化」，仅供手工构造 proposal 的测试使用。 | ✅ DONE 2026-08-28 — 冻结的 08-27 结果不变（SKHY/MU $30,000、RAM $1,905.66、总卖出 $31,842.02）。闸门 20 拦截回退到扁平化。**两种反向破坏均触发**：关掉 PASS A → 9 个测试红；PASS B 改用原始账面 → 8 个测试红 |
| R24 | **闸门 21：调用了但未定义的模块级名字。** | ✅ DONE 2026-08-28 — 我在改写 `_build_decisions` 时用整段替换切掉了它下面的 `_cash_routing_md` 与 `_followups_md`，而 `build()` 里两处调用还在——**`compileall` 通过，日报运行时会 NameError**。是闸门 7 的 B46 指纹撞出来的（**第二次**救我于同一类错误）。新增 AST 闸门直接查这类「导入通过、运行即死」，反向破坏验证：删函数留调用 → `compileall` 通过、闸门 21 FAIL |

**新增的组合级不变量**（`tests/invariants.py`）：`theme_ceiling_respected()`（所有 Decision 应用后主题敞口 ≤ 上限）与 `no_unnecessary_theme_cut()`（方案 (a) 只移除超额，故任何被主题规则绑定过的主题**必须正好落在上限上**；落到上限以下即为不必要的超额卖出——这条正是能抓住 08-28 那次「敞口被削到 0 而上限是 60,000」的检查）。

### R25 买入旁路闭环 + PASS C 同轮不重复卖（用户 2026-08-28 拍板）

**买入旁路**：卖出已进统一 Decision，买入没有。`pipeline.default_stages()` 没有 TIER_ENTRY；PASS C 遇到不在原始持仓里的 BUY ticker 时 `targets.get()` 为 None 后直接 `continue`，新标的**不可能**产生 BUY Decision；与此同时 `_followups_md()` 直接印「可考虑买 X 股 ≈ $Y，止损=入场−8%」、再入场提示直接印股数金额止损、`_candidates_md()` 印「1%风险示例股数」。**三个 Decision 之外的可执行数字产生点**——与三个卖出总额同一缺陷类，只是在买入侧、且还没被实盘邮件撞见。

| id | 项 | 状态 |
|----|----|------|
| R25 | **tier 7 `cockpit/rules/entry.py`**：候选与再入场先产出 RuleProposal，再经 pipeline 成 Decision。买入金额 = min(1% 风险预算 / $30,000 硬顶 / **卖出裁决执行后**的主题余量 / 已核实可用现金)，**绝不借钱**。`pipeline` 支持原始持仓中不存在的 ticker（初始 mv=0，可产生 BUY，进入最终 targets 供不变量与现金检查）。entry proposal 携带来源·数据时间·入场价·止损·风险预算·股数与金额的计算证据·主题归属·数据缺口·人工确认条件。 | ✅ DONE 2026-08-28 — 闸门 22/23，五种反向破坏均触发 |
| R26 | **现金闸门保守口径**：已核实现金 ≤ 0 时本报告不产生任何 BUY，**不依赖本轮建议卖出的预计回款**；热度 ≥6% 暂停新增风险时同样不产生 binding BUY。 | ✅ DONE 2026-08-28 — 测试含「本轮建议卖出 $20k+ 但现金仍为负 → 无 BUY」 |
| R27 | **未核实即不给数字**：Serenity 14 / VCP / 稀释三项必须各带来源与未过期的核实日期，缺一即只产生 WATCH，**不显示股数与金额**。 | ✅ DONE 2026-08-28 — **注意现状**：系统目前**不为候选计算这三项**，所以生产环境里每个候选都会落到 WATCH。这是诚实的现状，不是待掩盖的 bug；要产生真实 BUY，必须先把这三项的核实做成数据 |
| R28 | **可执行数字只出现在 action_list**。观察区保留评分、等待价、待核实条件、为什么现在不能买；删除「可考虑买 X 股」「约 $X」「再入场示例股数」「1%风险示例股数」及 `size_1pct_stop8` 字段本身。 | ✅ DONE 2026-08-28 — 闸门 23 用正则挡住这五种写法复活 |
| R29 | **PASS C 同轮不重复卖（正式拍板）**：同一报告周期、同一标的，若 tier 1-5 已产生减仓或清仓，本轮不得叠加利润止盈；止盈保留为 supporting reason，下一份报告基于执行后的新账面重新评估。① 无更高层减仓时止盈正常绑定；② 下一轮更高层约束解除后止盈可再次绑定；③ HARD_EXIT 始终优先。 | ✅ DONE 2026-08-28 — `TestProfitTakeSamePeriodRule` 四个测试逐条覆盖，含 supporting_rules 保留与 trace 里「next report」的说明 |

**当前测试与闸门状态（2026-08-28）**：`python3 tests/run.py` → **202 个测试全绿**；`python3 selfcheck.py` → PASS（23 道闸门）。闸门 11 = 整套回归测试纳入 CI；12 = 规则模块不得渲染输出；13 = 只允许一条尺寸计算路径（`_action_plan`/`_dispose_order`/`该减$` 列不得复活）；14 = intraday 隔离；15 = 安全边界；16 = 单票硬顶唯一定义；17 = 杠杆 ETF 硬止损已接线；18 = 热度语义不自相矛盾；19 = RS 不得进入主题分配路径；20 = 裁决必须走组合级顺序管线；21 = 调用了但未定义的名字；22 = 买入必须经 tier 7 裁决且新标的不得被丢弃；23 = 可执行数字只许出现在 action_list。**每一道新闸门都做过反向破坏验证**。`selfcheck` 的源码扫描已由 `cockpit/*.py` 改为**递归**——扁平 glob 会让所有闸门在代码搬进子包的那一刻静默失明，三个新 config 键刚被引入 `cockpit/engine/` 时警告就已经哑了。

**本轮已顺带确认的既有事实（非新缺陷）**：净值 ≠ 市值 + 现金，差额正是应计项（−171.31 利息 + 8.24 股息 = −163.07），系统从不建模应计；已结算现金 −23,644.30 远低于现金 −9,186.07，现金推演阶梯用后者会高估可用资金；盘中警报 workflow 已由用户在 Actions UI 停用，但 yml 的 cron、`config.alerts.intraday_cron_utc`、以及它那套 B48 之前的老止损口径（`max(200DMA, 成本×0.8)`）与未接返回值的 `notify.send()` 都还留在仓库里（用户 2026-08-28：可以清，但先别急）。

### P0A 生产买入关闭开关（用户 2026-09-01 拍板）

**为什么需要这个开关。** 统一 Decision 层的**卖出**一半（组合级顺序裁决、单票/主题上限、趋势止损、止盈、绩效台账）已按用户口径验收；**买入**一半没有。R27 已经记下现状：系统**不为候选计算** Serenity-14 / VCP / 稀释这三项，所以今天生产环境里每个候选都落到 WATCH——但那是**核实闸门恰好接住了它**，不是控制。只要有人给候选记录补上这三个字段，或改一行 config，一笔没人端到端验收过的买入就会出现在首屏。R25/R26/R27/R28 把买入**关进了 Decision 层**，但没有**关掉**它。

| id | 项 | 状态 |
|----|----|------|
| R30 | **`risk.entry_decisions_enabled` 开关，fail-closed。** 唯一解析入口 `cockpit/domain/policy.py::entry_decisions_enabled()`：只有字面 true（含 `"true"/"yes"/"on"/"1"`）才开启；**缺 key、config 为空、值为 0/1/`"False"`/`None` 一律视为关闭**，代码里不存在 `.get(KEY, True)` 这样的默认打开路径。关闭时：候选与再入场**仍然**走 tier 7、仍然产出 WATCH/REVIEW（理由里写明 P0A），但**永不**产生 binding BUY，不显示买入股数、金额、目标仓位或止损挂单。生产 `config.yaml` = false。 | ✅ DONE 2026-09-01 |
| R31 | **首屏必须说出来。** `daily_brief._buy_gate_reason()`（从 `build()` 里抽成纯函数，否则没有网络就没有任何测试能观察它）把 P0A 边界排在**所有市场条件之前**：现金为正、热度 3% 的日子，保证金规则和热度规则都不说话，只有 P0A 说话。首屏「今天是否允许买入」显示 **⛔ 否 —— P0A 仅启用持仓卖出/风控裁决；可执行买入等待 P0B 数据与规则验收。** | ✅ DONE 2026-09-01 |
| R32 | **测试夹具必须显式 opt-in。** `tests/test_entry_decisions.py::cfg()` 显式写 `entry_decisions_enabled=True`。这些夹具存在的意义是证明一笔完整 BUY Decision **能**被产出；它们不得依赖任何默认值，否则生产默认一旦被翻开，测试会替它掩护。 | ✅ DONE 2026-09-01 |

**P0A / P0B 边界（明确写下，避免把 P0B 当成已完成）**

- **P0A 已交付**：持仓卖出裁决、单票与主题集中度、杠杆 ETF 红线与硬止损、趋势/止损、止盈与同轮不重复卖、组合级顺序裁决（PASS A/B/C）、绩效台账（TWR + 含离场归因）、行动清单渲染、以及本节的生产买入关闭开关。
- **P0B 未开始**：完整买入引擎、再入场、**已结算现金**口径（现已确认已结算现金 −23,644.30 远低于现金 −9,186.07，买入闸门今天用的是后者）、候选三项核实的**数据生产**、entry hardening。**不得**因为 R25–R28 已 DONE 就把 P0B 当作完成——那几条做的是"把买入关进 Decision 层"，不是"买入可以用了"。
- **R18 仍然 OPEN**：完成定义仍是「真实邮件发出并与同期 IBKR 逐项核对」。P0A 没有发过任何邮件。

**反向破坏证据（每一条都实际跑过，观察到失败）**

| # | 破坏 | 观察到的结果 |
|---|------|-------------|
| 1 | `entry_decisions_enabled()` 改成 `.get(KEY, True)`（fail-open） | P2_FailsClosed **4 红** |
| 2 | 生产 `config.yaml` 翻成 `true` | P1 ×2 + P4 首屏 + P6 冻结账面 **4 红** |
| 3 | 从 `entry._gates()` 删掉 P0A 分支 | P2 ×1 + P3_FlagOffProducesNoBuy 全部 **7 红** |
| 4 | P0A 判断挪到 `_buy_gate_reason()` **最后** | P4_FirstScreenSaysNo.test_p0a_outranks_the_market_conditions **红** |
| 5 | 夹具去掉显式 `entry_decisions_enabled=True` | **18 红**（P5 ×2 + T2/T4/T6/T7/T8/T12 全部买入用例） |

⚠️ 第 4 次破坏我第一次构造**错了**：只在解析器调用**之前**插了一个不含市场条件文案的 `if`，行为完全没变，228 测试照样全绿。这与 R11 那次改 `market_value`（决策用 股数×价格，改它不影响结果）是同一类错误——**破坏必须真的改变答案**。改成"把 P0A 判断挪到最后"后才触发。也正因为这次，才把源码切片式断言换成对 `_buy_gate_reason()` 的行为断言：源码切片分不清"文案搬家了"和"文案没了"。

**`_to_delete/` 隔离（P7）**：三条断言（已加载模块路径 / tests 目录树 / `git ls-files`）**未做完整反向破坏**——本设备挂载拒绝 `unlink`，制造出来的污染删不掉。改为验证**探测器有牙**：喂一个 `__file__` 落在 `_to_delete/` 下的模块 → 被抓；同一段 walk 指向仓库根（真的有 `_to_delete/`）→ 被抓；`git ls-files -- cockpit` 返回 33 条 → 命令形式确实会报告被跟踪的路径。三条探测器均有效，但"真的把污染放进去再看它红"这一步没做。

**测试与闸门状态（2026-09-01）**：`python3 tests/run.py` → **228 个测试全绿**（202 + P0A 的 26 个）；`python3 selfcheck.py` → PASS（23 道闸门，未新增闸门）。

**CI 侧留痕**：闸门 11（整套回归测试纳入 CI）在 GitHub 上其实一直是红的——`.github/workflows/selfcheck.yml` 只装 `pyyaml`，而测试会 import `daily_brief` → `fmp` → `requests`，干净环境里 7 个用例直接 import 失败。2026-09-01 已在 `p0-decision-engine-checkpoint` 上单独修复（`39b14c6`：改装 `-r requirements.txt`，步骤名 7 gates → 23 gates），Actions 15 秒变绿。**这正是"该出现的东西从来没出现过"的又一例**：闸门写好了、本地绿了，但它在 CI 里从未真正跑过一次完整套件。

---

## P0 — 会按时发错 / 数据错误（最高优先）
| id | 项 | 详情 | 状态 |
|----|----|------|------|
| B1 | ~~biweekly_review.py 旧版~~ | 6/27 触发；缺 holdings_snapshot/IBKR真实盈亏、未用 position_caps 升级风控、无选股雷达。selfcheck 对此硬失败。 | ✅ DONE 2026-06-23 |
| B2 | ~~portfolio_heat_pct 算错+标错~~ | 我写成 Σ(超上限$)/净值；position-sizer 的"组合热度"=Σ(各仓到止损在险)/账户。还误给 <6-8% 阈值。已改为真实热度=Σ(在险到止损)/净值。 | ✅ DONE 2026-06-23 |
| B3 | ~~stop_review_level 对落后股无意义~~ | =max(200DMA, 成本×0.8)，落后股会高于现价→"止损"在现价之上、trivially 破位。已改：止损取现价下方最高者；全破→already_broken_down。 | ✅ DONE 2026-06-23 |
| B23 | **llm.py temperature 被新模型拒绝→双周复盘 LLM 段丢失**：2026-07-11 双周邮件报 400 `temperature is deprecated for this model`（opus-4-8），fail-open 只发出了雷达表。已改 llm.py：先带 temperature 调用，被拒即去掉 temperature 自动重试一次（B13 对 sonnet 日报仍生效）。 | ✅ DONE 2026-07-13 — 实弹验证：07-13 双周复盘 opus-4-8 LLM 段完整生成（temperature 被拒自动重试生效） |
| B25 | **config.holdings 与 live IBKR 严重漂移（结构性盲区）**：2026-07-11 live 共 15 个持仓、净值 $158.6k、现金仅 $126，而 config 只登记 NVDA/ORCL/MSFT/META(已平)。快照/风控表/热度/新闻/财报/EDGAR/盘中警报的宇宙全部取自 config → 约 $117k(≈74%) 持仓完全不被跟踪；MU $36.5k、SKHY $40.4k 均已超 $30k 硬顶但系统无感知；组合热度 2.0% 严重低估；雷达把已持有的 MU/MRVL 标为"未持有"候选；警报仍盯已平仓的 META。修法待拍板：①持仓改由 IBKR Flex 驱动（config 降级为角色注释） ②仅加 Flex↔config 漂移报警（selfcheck+日报顶部） ③手动更新 config（最快但会再漂移）。 | ✅ DONE 2026-07-13 — 实弹验证：07-13 日报 14 票全跟踪、真实热度 7.9% 顶格、漂移警报首跑即抓出 SKHY→SKHYV 错配（Flex 代码为 SKHYV，FMP 已核 SKHYV=SK hynix）；config 已随之修正 |
| B27 | **daily 的 state 回写从 6/29 起每天静默失败**：GitHub 上 nav_history/last_positions 冻结在 06-27，而 alert_state 已是 07-10——daily-brief.yml 的 push 没有 rebase 重试（`git push || echo skipped`），被盘中警报的频繁 push 挤掉(non-fast-forward)后静默吞掉；且 workflow 双向漂移（GitHub=整点cron 0 16+permissions.write；本地=错峰cron 23 16+无permissions）。后果：NAV 史断档 9 个交易日(双周业绩失真)、META 每天被重复判"新平仓"、反思记忆写入全部丢失。已交付合并版 daily-brief.yml（错峰 cron+permissions.write+rebase 重试，与 intraday 同款）。 | ✅ DONE 2026-07-18 — 实弹验证：nav_history 07-13~07-18 连续六日入库，rebase 重试在 intraday 推送竞争下全部成功；此前两次「未贴上」的判断是 raw.githubusercontent CDN 陈旧副本误导（教训：对 raw 的验证抓取必须带 cache-buster 参数） |

## P1 — 正确性 / 功能缺口
| id | 项 | 状态 |
|----|----|------|
| B4 | ~~biweekly 没算业绩~~ ✅ DONE 2026-06-24 — _performance() 用 NAV 历史算区间收益 vs SPY + alpha（近似净值收益，非入金调整 TWR）。 |
| B5 | 反思记忆**平仓自动写入**未接（memory.py docstring 还谎称会自动增长）。 | ✅ DONE 2026-06-23 — 平仓离场→自动追加反思教训(daily_brief) |
| B6 | ~~新日报对 live IBKR 再核对~~ ✅ DONE 2026-06-23 — 见下方「B6 最终核对结果」，去重/止损/热度/风控/稀释/平仓逐项对 live 验证通过。 |
| B7 | **死配置键**：vol_window_days/corr_window_days(且与1年EWMA矛盾)/news_max_age_days/dilution_atm_disqualifier/skip_us_holidays — 删除或接通。 | ✅ DONE 2026-06-23 — 删 vol/corr_window_days 死键, 加 hist_window_days, 接通 news_max_age/dilution 旗标 |
| B8 | **README 过时**（0 提及 EWMA/选股雷达/position_size/9段）——重写。 | ✅ DONE 2026-06-23 — README 重写 |
| B9 | 配置宪法 §3 持仓快照旧股数 + §5 风控描述非EWMA/兜底——标"以 IBKR 为准"并更新。 | ✅ DONE 2026-06-23 — 宪法 §3 标'以IBKR为准' §5 写EWMA/兜底 |
| B24 | 风控表 current_usd 用 Flex 前一日市值，与组合快照(FMP 现价)同一封邮件口径不一致：07-10 邮件 ORCL 快照 $17,861(@141.47) vs 风控表 $18,208(=前日收价 144.22)，TRIM 金额按旧价算。修法：daily/biweekly 给 position_caps 的 cur_mv 改为 股数×FMP现价（同 B20 口径）。 | ✅ DONE 2026-08-24 — 实弹验证：08-24 日报例外区 SKHY $47,659 与紧凑快照 $47,659 同价基（同为 股数×FMP现价）。原交付：两处在取到 quotes 后重建 cur_mv=股数×FMP现价，Flex mv 仅无报价兜底 |
| B42 | **日报瘦身·例外报告制（用户确认四诊断 2026-07-19）**：诊断=①同一事实重复3-5处 ②信息无视觉权重（正常与异常同版面）③LLM散文复述表格 ④低频信息每日全文重发。改造：邮件=标题+行动清单+三盏灯(热度/现金保证金/财报窗口)+例外区(仅破位/超限/距止损<5%/财报≤14天/稀释，健康票折叠一行)+LLM附录(仅重大消息≤3+异常点评2-4句+待验证≤5)+紧凑快照(6列)+雷达。宏观表/技术位全表/逐票检查清单整段删除；LLM max_tokens 4200→1800、bundle 95k→60k。 | ✅ DONE 2026-08-22 — 实弹验证：07-20 起新格式稳定运行（08-21 邮件核：行动清单+三盏灯+例外区+LLM附录+紧凑快照，首屏即决策） |
| B32 | **今日行动清单（决策层——用户核心诉求 2026-07-13：「系统没告诉我什么时候买什么卖什么、钱怎么用」）**：把 卖(超限 TRIM $金额/破位清仓评估)、买(候选 posture=buyable-on-support 且 cap 有空间 且 组合热度预算允许 → 入场区/止损价/股数)、否则明确写「今天不动」，组装成邮件**最顶部**的祈使句行动区；纯规则引擎代码直出（沿 B22 教训不经 LLM），每条附触发规则名。现状问题：同样的结论散落在第 5/6 节的描述性语言里（07-13 双周第 6 节其实已给出完整打法但用户读不出"要我做什么"）。 | ✅ DONE 2026-08-22 — 实弹验证：行动区每日置顶输出（08-21：⛔违规+四笔减仓$+热度闸门+等回调候选，每条附规则名） |
| B33 | **持仓名单改 IBKR 驱动（B25 方案①升级，用户拍板 2026-07-13）**：daily/biweekly/intraday 三处 holdings 改为 live Flex 持仓−exclude；config.holdings 降级为角色注释+IBKR 掉线兜底；quotes 宇宙并入 live 持仓；漂移警报改为 🟡「config 注释未同步」提醒（不再是失明警报）。买新票次日自动进快照/风控/警报/行动清单，无需改 config。仍受 Flex EOD 滞后约束（B21）。 | ✅ DONE 2026-08-22 — 实弹验证：新持仓 RAM/SKHY 未改 config 即自动进快照/风控/行动清单（🟡 注释提醒生效，07-20 起未再升红色漂移警报） |
| B28 | **全市场新主线扫描器（地图外发现，用户 2026-07-11 拍板要做）**：雷达现在只扫 config 手画的 8 个子板块，地图外的下一个 SNDK 不可见。规格：宇宙=美股普通股 mcap≥$2B、价≥$10、日均成交额≥$50M；信号=距 52 周新高 ≤5% + RS vs SPY(63/126日) 前十分位；按 industry 聚类，同业 ≥3 只贴近新高 = 疑似新主线簇；输出为雷达新增小节「地图外新主线候选」（代码直出，沿 B22 教训不经 LLM），每票标 posture（ext描述仍适用：发现≠追高）；同一簇持续 ≥5 交易日 → 提示把该 industry 加进 config.subthemes。成本：1 次 FMP screener + 批量 quote，挂在 daily_brief 内。 | ✅ 交付 2026-08-22（Phase C）：新模块 `cockpit/scanner.py`——FMP screener 宇宙(≥$2B/$10/$50M日成交额，周刷新，排除地图内票)→每日轮扫 250 票分片(Starter 额度友好，全宇宙 3-5 日一轮)→贴近新高≤5%+RS(vs200−SPY)≥20 命中池持久化→同业≥3 聚类→簇持续≥5天建议入 subthemes；日报末尾🛰️代码直出小节；state/scanner_state.json 由 workflow 提交。与规格的两处偏差(已声明)：RS 用 vs200 代理而非 63/126 日双窗(报价单次调用可得，口径与雷达一致)；top-decile 改为绝对阈值(分片扫描下分位数不可得)。验收=首封日报出现🛰️小节(首跑建立宇宙，2-5 日后命中池饱和) — ✅ DONE 2026-08-24：08-24 日报 🛰️ 段出现（宇宙 526 票 / 轮扫 250 / 命中 13 / 今日无聚类） |
| B29 | **依从性记分板（我建议的下一开发项，与 B28 先后待用户确认）**：biweekly 自动统计过去两周每个系统信号（TRIM/破位/等回调/超限/漂移）× 用户实际动作（对照 IBKR 持仓变动）× 事后盈亏，直接回答「听系统 vs 不听系统各赚亏多少」。依赖：B27 state 持久化修复生效（last_positions/nav 连续）。这是回答「系统值不值得继续」的唯一数据化途径。 | ✅ DONE 2026-08-22 — 实弹验证：08-22 双周邮件记分板段生成（07-20 起 8 信号/执行 0/无视 8，无视净代价 −$10,809 双向如实） |
| B44 | **再入场规则（用户 Q2 痛点：止损后无回场之路，2026-08-22 拍板）**：任何整仓离场的票自动进 state/reentry_watch.json 观察池（有效期 reentry_watch_days=90 天）；当其收盘重新站上 50 日线 → 行动清单🔁段给出再入场提示（全新 1% 风险股数+止损−8%，热度超预算时标注先减后买）；提示一次不重复。止损→踏空由此变成有界的规则化往返。 | 🔴 OPEN — 代码已交付 2026-08-22（`_reentry_update`）；验收=下次有持仓离场后，回升站上50日线当日日报出现🔁段 |
| B45 | **止盈阶梯（用户 Q8 缺口：系统有止损无止盈，2026-08-22 拍板）**：持仓浮盈 ≥ profit_take_trigger_pct(默认25%) → 行动清单💰段提示 二选一：止盈减 profit_take_fraction(默认1/3) 或 止损上移至10日低（锁一部分、余仓不封顶）。v1 单档，多级阶梯（每+25%再提示）留 v2。 | 🔴 OPEN — 代码已交付 2026-08-22；验收=下次有持仓浮盈≥25%当日日报出现💰段 |
| B49 | **业绩归因（反应式排查发现 2026-08-23）**：跑了 3 期双周复盘，用户仍无法回答「我到底靠什么赚/亏的钱」。现状：`_performance()` 只给区间净值收益 vs SPY，且**未做出入金调整**（用户期间加过保证金、转过账 → 数字被污染，08-22 那期 +8.03% 含此失真），更没有任何**归因分解**。B29 记分板回答的是「听不听系统」，不是「钱从哪来的」。规格：① 出入金调整的 TWR（IBKR Flex 可取现金流）；② 三分归因——选股贡献（选中的票本身涨跌）/ 尺寸贡献（权重分配对不对）/ 择时贡献（进出场时点）；③ 双周复盘第(1)节替换为归因表。 | ✅ DONE 2026-08-27 — `_attribution` / `_attribution_md`（🧮 段，代码直出不经 LLM）。**关键发现：不需要 Flex 现金流查询**——`signal_history.json` 自 2026-07-20 起存了逐日 `net_liq` + 每只持仓的 `{shares, price}`，据此可把 NAV 变动拆成 市场盈亏 `Σ shares(t-1)×Δpx` 与 残差（出入金/利息/费用），交易不改变 NAV 故自动抵消。TWR = Π(1 + 当日市场盈亏/前日NAV)。三分归因：选股=等权收益、尺寸=实际权重−等权、择时=实际−首日股数冻结。**冒烟（15 个交易日实数据）**：净值 +0.78% / TWR +0.42% / 市场盈亏 $+1,180 / 残差 $+24；选股 −2.82%、尺寸 **+2.96%**、择时 −$38 —— 即这段时间**等权持有这批票是亏的，是权重分配把它拉回正**。**交叉校验**：市场盈亏 + 残差 = NAV 变动，误差 0.00。口径边界已写进渲染：窗口非成立以来、B57 之前的价格是盘中快照、不含佣金滑价、残差不是干净现金流。 |
| B50 | **离场后跟踪（反应式排查发现 2026-08-23）**：系统只记录平仓（B5），从不测量**离场决定本身对不对**。后果是结构性偏差——B29 只算「无视卖出信号的代价」，从不算「卖早了的代价」，等于系统只会说"你该卖"永远不会说"你卖早了"。规格：平仓后把该票挂进跟踪池 N 天（沿用 B44 的 reentry_watch 数据结构），双周复盘报「离场后 ±X%」，正负如实——与 B29 同一诚实标准。 | ✅ DONE 2026-08-27 — `_exit_tracking` / `_exit_track_md`（🔭 段，接入双周复盘）。四处改动：① **近乎清仓也算离场**：股数砍到 ≤10% 即记（2026-08-26 MRVL 54.741→0.741 股，对当时**所有**卖出侧规则都不可见——不算 closed，所以没复盘、没进台账、没跟踪）；② 台账测量窗口 `exit_track_days=365` 与 B44 提示窗口 `reentry_watch_days=90` **分开**（半年前离场的票再提示是噪音，但「那次卖对了吗」半年后仍值得算）；③ **离场票并入 `_universe()`** —— 否则 SPCX 这种既不属任何 subtheme、又已不在持仓的票拿不到报价，会被静默丢掉（Rule 6 那类失败）；④ `tools/b50_seed_exit_ledger.py` 一次性回填 B44 上线前的三笔离场。**首次实跑（2026-08-27 实时价）：MSFT +10.6% / SPCX +7.3% / TSM +0.6%，三笔全部「卖早了」，中位数 +7.3%。** 回填行标 ⚠️：离场价用的是最后观察到的快照价而非成交价。 |
| B51 | **止损规则历史回放（反应式排查发现 2026-08-23，直接回答用户 Q2「止损后暴涨怎么办」）**：系统里所有参数——RS≥20 高信念档、20日低止损、40% 主题线、6-8% 热度预算、入场−8%——**没有一个被历史验证过**，全是从书/文章采纳的"听起来合理"的数字。规格（**目标是体检不是调参**，防过拟合）：用 FMP 历史把 20日低止损规则回放到用户**自己的 13 只持仓**过去 6-12 个月，统计：被洗出次数、洗出后 20 日内的涨跌、若不止损的最大回撤。用他自己的股票回答"会不会被洗"，比我讲道理有说服力。 | ✅ DONE 2026-08-27 — 工具 `tools/b51_stop_replay.py`（手动分析，**不接入日报管线**：目的是体检不是调参）。样本 11 只 × 约一年（SKHY/RAM 上市不足一年、CCXI 合并前是壳，剔除）。**82 次触发**。**① 用户的直觉是对的**：卖出后 20 日 **72% 在涨**，中位数 **+6.8%**，最好 +61.1% —— 「刚卖就反弹」是常态不是例外。**② 但止损确实救过**：37%（30/82）的触发在其后 20 日内还会再跌 >10%，最差 −27.7%。**③ 期末收益对照（无止损 / 止损不再入 / 止损+B44再入场）**：MU 373/+4.5/216、AAOI 286/−12.6/247、ASX 230/215/163、MRVL 172/−12/97、SOXX 84/−2.8/49、KLAC 79/+7.6/98、GLW 76/−3.7/76、NVDA 21/−7.1/−22、AVGO 3.4/−8.6/−27、ORCL **−52/−10/−10**。**结论（不是调参建议）**：在这一年的单边牛市里，「止损不再入」几乎必然最差；**「止损+再入场」是否值得，完全取决于个股** —— 它救了 ORCL（−52% → −10%）和 KLAC，却把 NVDA（+21% → −22%）和 AVGO（+3% → −27%）做成了亏损。**真正的教训不是「该不该止损」，而是「止损的代价随波动率和趋势质量剧烈变化，用一个固定规则套所有票必然在某些票上很贵」。****④ 副产品：回放直接抓出 B58**（生产口径 82 次触发一次都不会报）。 |
| B52 | **CI 闸门：selfcheck 从未在真正重要的地方跑（反应式排查发现 2026-08-23）**：`selfcheck.py` 只在我的容器里跑；用户手动粘贴到 GitHub 后**线上从无任何自动校验**——一次粘错（历史上真发生过：risk.py 里是 llm.py 的内容）会静默进入生产，直到某封邮件出错才发现。规格：新增 `.github/workflows/selfcheck.yml`，push 即跑 `python3 selfcheck.py`，失败则 GitHub 发通知邮件。成本≈0，是当前性价比最高的一项。 | ✅ DONE 2026-08-24 — 实弹验证：`.github/workflows/selfcheck.yml` 已在 main（提交 38d98d3），Actions 页两次运行全绿（#1 38d98d3 / #2 75e054f，2026-08-24 打开页面逐条核实）。原交付说明：workflow 含 6 步（checkout / py3.11 / 只装 pyyaml 无网络依赖 / selfcheck 7 闸门 / compileall 全模块 / config.yaml 顶层键校验），`paths-ignore: state/**` 避开 daily 的状态提交，timeout 5 分钟。三步已在容器内预演通过。**须由用户在 GitHub 网页手工创建**（`.github/workflows/*` 在设备端受保护、我写不进去）。验收=创建后首次 push 在 Actions 页出现绿色的「自检闸门 Selfcheck」运行 |
| B48 | **持仓体检表（把 2026-08-23 手动处置手册变成日报常驻段；用户 2026-08-23 提出"为什么系统做不到"）**：手册里的 5 项计算，代码逐条核对后确认系统**一项都不做**——① 止损口径不同：`_holdings_snapshot` L114 用 `max(200DMA, 成本x0.8)`，不是 20 日低（对高波动票 200DMA 往往远在下方或已破，成本x0.8 是锚定成本的伪止损）；② `position_size()` 只在 L260(B44再入场) 和 L518(候选) 被调用，**从不作用于现有持仓**——所以系统能说"新票买几股"，却从不说"你手上这只该拿几股"；③ 组合热度用自己的止损口径算，与"能扛住波动的止损"不是一回事（实测差异：系统口径 12.5% vs 20日低口径 22.5%）；④ 无层内 RS 排名，`_theme_exposure`(B36) 只合计敞口、不比较层内强弱，所以从不说"这只是本层最弱、该它先走"。**规格**：日报新增「🩺 持仓体检」代码直出段，每票给 20日低止损价／该止损下在险$／占净值%／层内RS排名(第N/M)+本层最弱标记／按风险档(RS≥20用2%，其余1%)反推的目标仓位$／该减$；组合层给双口径总在险对比；卖出建议按理由硬度排序(重复>体系外>层内最弱>破位>超限尺寸)并附现金推演阶梯。**待拍板子项**：系统主止损口径是否整体切到 20 日低（还是双口径并列取更近者）。 | ✅ DONE 2026-08-24 — 实弹验证：08-24 日报同时出现 🩺 体检表与 🪜 处置排序（总在险 21.6%、该减 $90,221）。**同封邮件暴露 B53**（层归属缺失→SKHY 被判「体系外·清仓」）。原交付说明：`_holdings_snapshot` 执行止损切到 **20日收盘低×0.99**（史缺时 fail-open 回旧规则），200日线降级为独立 `below_200dma` 趋势旗标（不丢信息）；新增 `_position_audit`/`_audit_md`（🩺 体检表：层内RS排名+🔻本层最弱+📉破200日线+🚨距止损<5%+在险$/%+风险档+目标仓位+该减$）与 `_dispose_order`（🪜 按理由硬度排序+现金推演）；daily 与 biweekly 双端接入。**冒烟发现并修掉一处危险缺陷**：止损仅 1-4% 的票反推出 $30k「目标仓位」，读起来像建议加仓——已强制 `target=min(target, 现仓)`，本表只答「该减多少」，并加 🚨 与「在险$小≠安全」提示。config 新增 high_conviction_rs/risk_pct_high/risk_pct_base。实测（08-23 真实持仓）：总在险 **24.2%** vs 预算 6-8%，该减合计 **$84,583**。验收=下封日报出现 🩺+🪜 两段 |
| B53 | **层归属缺失让 B36 静默失效、并让 B48 把最大持仓判成「体系外·清仓」（2026-08-24 实弹发现）**：`_theme_of()` 只读 `config.subthemes[*].names`，SKHY/KLAC/ASX/GLW/CCXI/SOXX 六只都不在里面（SOXX 只在 `etfs`，SK 海力士在选股规格里原本是「经 EWY」代理），于是 `_layer_of` 把它们全部打成 `"unmapped"`。后果三条：① `_theme_exposure` 里 `if pct>=thr and th!="unmapped"` **显式豁免 unmapped** → 系统看到的 memory_hbm 只有 31.3%，真实（MU+SKHY+RAM×2）是 62.8%，**B36 自上线起从未触发过**（08-21、08-24 两封邮件均无 🟣，实证）；② `_dispose_order` 把 `unmapped` 读成「体系外/无框架容纳」并给**整只清仓**金额 → 08-24 日报第 1 条是「SKHY 清仓 $47,659」，而 SKHY 是内存双雄之一、29.7% 净值；③ 🩺 层内排名把 SK hynix / Corning / 一个 SPAC 空壳放进同一个假层比强弱（SKHY 被标 🔻本层最弱 6/6）。另：`drift_extra` 只比 `config.holdings`，08-24 config 补全 holdings 后那盏唯一提醒「待补子板块归属」的 🟡 黄灯**消失了**，缺口反而更隐蔽。 | ✅ DONE 2026-08-24 — ① config `risk.theme_overrides` 补全六只层归属，CCXI→`outside_framework`（唯一真体系外）；② `daily_brief.py` 新增 `OUTSIDE_LAYER` 常量，`_dispose_order` 的「体系外」只认 `outside_framework`，`unmapped` 改为渲染 ⚠️ 数据缺口提示；新增 `amount()`：硬度 1-2 给整只清仓额、3-5 只给该减额（旧版用 `cut_usd` 当过滤器，**已经够小但理由最硬的票 CCXI/AVGO/GLW/RAM 全部落榜**——冒烟测试抓到的假阴性）；③ `selfcheck.py` 新增第 8 道闸门：任何 `config.holdings` 的票在 `subthemes.names` 与 `theme_overrides` 里都找不到即 **FAIL**，把这个漏洞变成机械拦截。冒烟（08-24 实时数据 + 真代码）：🟣 memory_hbm 62.8% 触发；🪜 变为 CCXI/KLAC/GLW/RAM/AVGO 清仓 → ORCL 减 → SKHY/MU 减尺寸；selfcheck PASS |
| B54 | **config 的角色注释是未经核实的自由文本，却在驱动真实的卖出建议（2026-08-25 用户当场抓到）**：`config.holdings[].role` 里写着「CCXI = Churchill Capital XI（SPAC空壳，体系外）」，我据此把它映射成 `theme_overrides: CCXI: outside_framework`，于是 B48 的 🪜 段把它排在**清仓第一位**、理由「体系外/无框架容纳」。**事实是**：CCXI 已于 2026-06-24 与 **Agility Robotics**（人形机器人 / Physical AI，Digit 已部署 Amazon/Toyota/GXO）签订 $2.5B 最终合并协议，2026-07-14 保密提交 S-4。它有明确标的、明确赛道，不是盲池空壳。**同批查出的还有两个**：SPCX 我说成「SPAC/新股 ETF」，实为 **SpaceX**（2026-06-12 IPO，市值 $1.77T）；RKLB 我归成「与AI主线无关」，实为 Rocket Lab 航天。三个错误的共同点：**我用 ticker 字面 + 项目内部旧注释推断公司是什么，从未打开过任何一手档案**。根因不是「没读文件」（config 我读了），而是**把内部文档当成了关于外部世界的权威源**——正是用户完整性核对准则第 1 条禁止的事。 | ✅ DONE 2026-08-27 — config 修于 2026-08-25：CCXI 角色注释改为事实并标注更正；`theme_overrides` 改 `CCXI: physical_ai`（不进 `subthemes.names`——单成员层会污染雷达的板块强弱表）。冒烟：CCXI 层归属 = physical_ai，且**从 🪜 中消失**（规则层面本就没有卖它的理由，之前那条理由是假的）；selfcheck PASS。 **13 只已全量核实 2026-08-25**（FMP profile + identity-event 新闻逐票过）：8 只 OK，**5 只 STALE 已改**——ORCL（漏了债务 $104B→$156B 的 AI capex 加杠杆，CEO 已换 Michael D. Sicilia）、AVGO（只写了半导体，漏了 VMware 基础设施软件那一半）、KLAC（漏了 2026-06-12 的 10:1 拆股，正是日报「流通股 +890% 疑似稀释」的真因）、ASX（「低毛利环节」标签与先进封装占比上升矛盾，不能再用它直接判该走）、GLW（Corning 是五大板块，不是纯光通信，原标签高估了它对光模块周期的贝塔）。SKHY/RAM/SOXX/ASX 的载体属性也已确认（SKHY=真 ADR 非合成品；RAM=2x of Roundhill Memory ETF「DRAM」，两者是同一敞口的 1x/2x）。 ✅ **补完 2026-08-27**：selfcheck 新增**第 9 道闸门**——`holdings[].role` 必须含「核实 YYYY-MM-DD，源=...」或明写「未核实」，否则 FAIL。上线即抓到：13 只里 **7 只（NVDA/MU/SKHY/MRVL/SOXX/CCXI/RAM）我 08-25 确实核实过，却没留机器可读的痕迹**——机器分不清「核过」与「没核过」。已按当时审计结果补齐（含 SKHY=真ADR非合成品、SOXX=跟踪 NYSE/ICE 指数非 PHLX SOX、RAM=Roundhill Memory ETF「DRAM」的 2 倍日收益）。反向测试：抹掉任一核实戳即 FAIL。**记忆里的 Rule 5 只约束我，闸门约束每一个会话。** 原「仍未做」的第 ② 项到此关闭；③ Physical AI 是否立第 11 层仍见 B47 v2 Q8。旧文：② 没有任何机制阻止下一条未核实注释进入 config——需要一条规则：**角色注释里凡是关于「这家公司是做什么的」的断言，必须附核实日期与来源（FMP profile / SEC / 公司官网），否则只写「未核实」**；③ 是否把 Physical AI 立为十层框架的第 11 层，见 B47 v2 Q8 |
| B55 | **🛰️ 地图外扫描段只报数字、不报名字（2026-08-25 发现）**：`scanner.daily_scan` 在无聚类时只输出「命中 N 票」，**不列出是哪 N 只**。08-24 那封日报写「命中 13 票 / 今日无聚类」，用户从邮件里得不到任何一个代码——而 `state/scanner_state.json` 的 `hits` 其实每条都带 `industry` / `rs` / `off_high` / `first_seen`，信息是有的，只是没渲染出来。后果：① 用户拿不到可执行信息；② 我要归类新标的必须去 repo 读 state 文件，绕了一圈。**规格**：无聚类时也列出**当日新增**（`first_seen == today`）的命中，按 RS 降序取前 8，每条给 代码/行业/RS/距高%；已存在多日的老命中折叠成一行计数。**顺带**：这是「新标的自动归类规则」（`00_README` §5.1）的上游数据源，列出来之后我不必再读 state 文件。**注**：把新名字写进 IBKR 自选清单这一步**不能代码化**——GHA runner 只有 Flex（只读持仓），没有 watchlist 写权限，那是会话内连接器的能力。所以 B55 只做「把名字渲染出来」，归类动作留在会话里按 §5.1 执行。 | ✅ DONE 2026-08-27 — `scanner.daily_scan` 新增「本日新增命中」表（`first_seen == today`，按 RS 降序取前 8，给 代码/行业/RS/距高%），**有簇无簇两条分支都渲染**；往日留存的老命中折叠成一行计数。冒烟（线上 scanner_state，2026-08-26）：命中池 26 只 → 正确识别出当日新增 3 只（GH +42.0 / ZETA +37.0 / CNC +26.6），其余 23 只折叠。 |
| B56 | **IBKR 订单域端点不可用——系统永远无法确认止损单是否挂上（外部阻塞）**：`get_account_orders` 自 2026-08-23 起**至少 10 次调用全部失败**，错误文本是完全通用的 `Error invoking method: An error occurred. Please try again later.`（无错误码、无细节）。同一秒内 positions / balances / summary / 报价 / watchlist 读写 / order_instructions **全部正常**，所以不是认证、网络或限流。**同源异常**：`get_account_trades` 返回 `{"trades": []}`——**成功但为空**——而当日账户确实有成交（2026-08-26 MRVL 部分离场，持仓股数与现金余额双向确认）。即订单域两个端点都不正常，方向不同：orders 报错，trades 静默返回空。**后果**：① 用户是否挂了 GTC 止损单，系统**只能靠他口述**，无法核实——这是 B48「止损纪律」整套设计的一个盲点；② 成交只能靠 positions+cash 差额倒推（2026-08-26 那笔 MRVL 就是这么发现的，他并未主动告知）；③ 「完成定义」里的「与实时 IBKR 对账」在订单这一维度上做不到。 | 🔴 OPEN（外部依赖）— 2026-08-26 已通过连接器 `provide_customer_feedback` 向 IBKR 提交完整 bug 报告（含端点名/错误原文/失败频次/同会话正常端点对照/trades 空数组异常/三条修复请求），已收到回执。连接器版本记录 v1.0.0→v1.1.6（最新 2026-07-30）未提及该问题。**在修好之前，止损单状态一律标注「用户口述，未经系统核实」，不得写成已核实。** |
| B57 | **日报从盘中改到盘后（用户 2026-08-27 提出）**：原 cron `23 16 * * 1-5` UTC = 12:23 ET **盘中**，邮件落到台北 00:50 —— 用户在睡觉，从不看；且邮件顶部长期挂着「盘中快照：未收盘，勿当收盘复盘」，**等于每天推送一份自称不能当复盘用的复盘**。更实质的问题是口径：20日低、止损位、距止损%、RS、组合热度全部取自盘中瞬时价。 | ✅ config 已改 2026-08-27：`30 22 * * 1-5` UTC（夏令时收盘+150分 / 冬令时收盘+90分，**一个设置覆盖冬夏令时**；台北固定 UTC+8 → 次日约 07:00 收到）。`calendars.market_phase` 早有 `postmarket` 分支、`PHASE_GUARDRAIL` 也写好了，标题的「盘中快照」是 `if phase == "intraday"` 才出现，**代码零改动**。 🔴 **未完成**：真正生效的 cron 在 `.github/workflows/daily-brief.yml`（设备写保护），**须用户在 github.com 手改**；改完前 config 与 workflow 不一致。配套建议（未做）：IBKR Flex query 周期 `Last Business Day` → `Today`，否则盘后复盘仍用前一日持仓（B21）。 |
| B58 | **`already_broken_down` 自 B48 起恒为 False —— 破位信号整条链路死了 41 天（2026-08-27 由 B51 回放抓出）**：`_holdings_snapshot` 用 `stop_level = min(hist[:20])*0.99` 且 `already_broken = price <= stop_level`，而 `hist[:20]` **包含当日价本身**。因 `min(...) <= price` 恒成立，`price <= min*0.99` **在数学上不可能为真**。B51 回放实测：挂单口径一年内 **82 次真实破位，生产口径 0 次**。**下游全部静默**：例外区不会因破位列票（L450）、行动清单不会出「破位清仓评估」（L525）、快照不会标「破位」（L483）、B29 记分板的「破位」信号类别恒为空（L211）、LLM bundle 的 `broken_down_holdings` 恒为空（L646）。B48（2026-08-23）换止损口径时替换掉了旧规则（`price` 同时低于 200日线与成本×0.8，**那个是能触发的**），引入本缺陷。 | ✅ DONE 2026-08-27 — 破位判定改用**前 20 根**收盘（`min(hist[1:21])*0.99`），即 GTC 单实际会坐在的位置；**显示用的 `stop_level` 保持不变**（含当日窗口是「明天该挂在哪」的前瞻位，本来就对）。双向冒烟：ORCL/AVGO/NVDA 的历史真实破位日 → 新逻辑 `True`、旧逻辑 `False`；MU/KLAC 正常持有日 → 不误报。selfcheck PASS。**教训与 B36 同源**：「代码已交付」不等于「功能在跑」，而这次连日报都看不出异常——因为它的表现形式是**该出现的东西从来没出现过**。 |
| B59 | **邮件发送失败是静默的——2026-08-26/27 连续两天没有日报，而 Actions 全绿（2026-08-28 用户发现）**：`notify.send()` 捕获所有异常、返回 False，而 `daily_brief.main()` 与 `biweekly_review.main()` **都丢弃了返回值**，于是 SMTP 失败 = 绿色运行 + 无邮件 + 无任何告警。**根因（已定位到分钟）**：2026-08-26 16:20 UTC 用户收到 Google「您的密码已更改」安全提醒；**改 Google 账号密码会作废全部应用专用密码**，GitHub Secrets 里的 `EMAIL_PASSWORD` 自此失效。当天日报在 18:06 UTC 推了 state（nav/signal 都有 08-26 记录）却没发出邮件——**执行成功、投递失败、无人知晓**。**另一个独立缺口**：08-27 连 state 都没有（nav_history 与 signal_history 均无 08-27），即当天**根本没运行**；cron 当时仍是 16:23 UTC（yml 改动在 16:55 UTC 之后），最可能是 GHA 调度被丢弃（官方文档明说 scheduled workflow 是尽力而为，B43 的 08-06 事故同源），**但我无法从 Actions 页确认——抓取工具把时长读成了时间，拿不到运行日期**。 | ✅ 代码已修 2026-08-28：两个 `main()` 改为接住 `notify.send()` 的返回值，失败即 `sys.exit(1)` → 运行变红，**GitHub 自己的失败通知不依赖这个 Gmail 应用密码，所以警报仍能送达**。 🔴 **（当时）未完成，须用户手动**（已被本行末尾 08-28 闭环取代）：① 重新生成 Gmail 应用专用密码并更新 GitHub Secret `EMAIL_PASSWORD`——**在此之前一封邮件都不会有**；② 在 github.com 给 `daily-brief.yml` 与 `biweekly-review.yml` 的「Persist state」步骤加 `if: always()`，否则发送失败导致的红色运行会连带跳过 state 提交、丢掉当天 nav（设备写保护，我改不了）。**教训与 B36/B58 同源**：这已是本项目第三个「该出现的东西从来没出现过」类缺陷——前两个是功能静默失效，这个是**投递**静默失效。凡是 fail-open 的出口，都必须有一个朝外的响声。 |  ✅ **已闭环 2026-08-28**：① 用户重生成 Gmail 应用专用密码并更新 GitHub Secret，当日 06:55 UTC 日报实际到达收件箱（Gmail 实查 `subject:"daily brief" newer_than:2d` 命中 1 封，08-26/27 确认为 0 封）；② `daily-brief.yml` 的 Persist state 步骤已加 `if: always()`（origin/main 已核）。**我上一轮的指令里有一条是错的**：我要求同时给 `biweekly-review.yml` 加 `if: always()`，但该文件**根本没有 Persist state 步骤**（全文 29 行），且 `biweekly_review.py` 不写任何 state 文件——我没打开文件就下了指令，与 B54 同源。08-27 整天未运行的原因**仍未确认**（Actions 页运行时间戳我抓不到）。 🔴 **本条修复引出新缺陷 B60**：运行变红 + `if: always()` 合起来，反而保证了一次性内容在投递失败时必被消耗。
| B60 | **一次性内容会被「没送到的那封邮件」吃掉——NVDA/AVGO 清仓复盘与 NVDA 再入场提示当场丢失（2026-08-28 核验时发现）**：08-28 有两次运行。06:14 UTC 那次检测到 NVDA/AVGO 已清仓，把 `state/last_positions.json` 覆盖为新快照、并把 `reentry_watch["NVDA"]["prompted"]` 盖上 `2026-08-28` 戳，**然后它的邮件静默失败**（应用密码当时尚未更新）。06:55 UTC 那次拿着**已经被更新过**的快照去比对，自然「没有平仓」，于是 🔁 再入场段与清仓复盘段一个字都没出现在用户收到的那封日报里。证据：`reentry_watch.json` 里 NVDA 记着 `prompted: 2026-08-28`，而 Gmail 里 08-28 只有 06:55 那一封、全文无 NVDA/AVGO 字样。**结构性根因**：`_reflect_on_closes` / `_reentry_update` 在**构建正文阶段**就写盘，而 `notify.send()` 在最后——写盘早于投递。**B59 与 `if: always()` 让它变得更糟而不是更好**：运行变红了，但消耗掉内容的那次 state 提交照样发生。这是本项目第四个「该出现的东西从来没出现过」类缺陷，前三个（B36/B58/B59）是功能或投递静默失效，这个是**内容被提前消费**。 | ✅ DONE 2026-08-28 — 引入延迟写机制：`_defer()` 把**消耗型**写盘排队，`flush_pending_writes()` 只在 `notify.send()` 返回 True 之后执行。被延迟的三处：`last_positions.json` 快照、`reentry_watch[*].prompted` 戳、`mem.save()`（ReflectionMemory.add **无去重**，重跑会造重复课程）。**测量型**写盘（nav_history / signal_history / scanner_state）**不延迟**——每次运行都必须留痕。`reentry_watch` 的新条目与过期裁剪也立即落盘（那是测量，不是提示）。四向冒烟：① 未投递 → 快照不变、prompted 仍为 null、memory 未增；② 次日重跑仍能检出同一笔平仓；③ 投递成功 → 三者一次性写入；④ 已消费的提示不再重复。**selfcheck 新增第 10 道闸门**：`main()` 必须真调 `flush_pending_writes()`（首版闸门被 `# flush_pending_writes()` 注释骗过——**会被注释骗过的检查等于没检查**，已改为逐行判断），且 `last_positions` 立即写、`prompted` 立即戳这两种回退写法出现即 FAIL；三向反测均能咬住。同时补齐 gate 7 的 DONE 指纹：B49/B50/B51/B53/B54/B55/B57/B58/B59/B60（B1/B2/B3/B6/B9/B23/B25/B27/B40/B52 **仍未补，已在 selfcheck 里写明这是已知覆盖缺口**）。**补偿动作**：NVDA 的 `prompted` 已手工改回 null，下一封**送达**的日报会重新给出再入场提示；`last_positions.json` **不回滚**——L19/L20 两条清仓课程已在 `reflection_memory.json` 里，回滚只会造重复课程；那两笔复盘改由本轮对话直接补给用户。 |
| B47 | **AI-DC 推理周期十层研究计划（Phase B，用户 2026-08-23 拍板，每周迭代）**：活文档 `AI数据中心研究_十层框架_v1.md`（工作区，不入 repo）。v1 已建：十层×五变量框架、30 家代表公司定价扫描、产业证据锚定（推理占比 2026 ~66%／首次超训练／token 成本年降 60-70% 为反向力量）、候选 7 只（GEV/ETN/ONTO/ALAB/CIEN/ET/CCJ，均未做基本面核查）、v2 问题清单 Q1-Q7。研究结论已回灌 config：新增 networking_dc/power_equipment/adv_packaging/energy_gas 四层 + ai_apps。**v2 首要任务 = Q1 电力层（VST −15.6%／CEG −22.8%／NRG −29.0% YTD 集体下杀 vs 90TWh 需求为真）的原因深挖，本周内完成**。 | 🔴 OPEN — v1 交付 2026-08-23；每周更新一版 |
| B46 | **卖出所得流向指令（用户 Q8 缺口：钱去哪没人说，2026-08-22 拍板）**：行动清单卖/减段后自动追加💸资金优先级行——负现金时：还保证金→清债前不开新仓/不补杠杆(红线v2③)→热度回预算→等待价分批→弹药；现金为正时同序去掉还债项。金额=当日建议减仓合计。 | ✅ DONE 2026-08-24 — 实弹验证：08-24 日报 💸 段出现（合计 $68,402 → 还保证金→不开新仓→热度回预算→等待价分批） |

## P2 — 暂缓 / 小项
| id | 项 |
|----|----|
| B10 | ~~生命周期阈值偏激进~~ ✅ DONE 2026-06-23 — 改用乖离(距50日)+距高点双轴；过热=大幅超50日线且贴近高点(抛物线)；实测杀跌日不再误标过热，trending/correcting 有区分。 |
| B11 | ~~风控乘数用 avg_corr 非 max_corr~~ ✅ DONE 2026-06-24 — eff_corr=max(avg_corr, 0.85×max_corr)，单一高相关 peer 也会收紧上限；输出新增 eff_corr 字段。 |
| B12 | ~~calendars 盘段 UTC 边界冬令时差 1 小时~~ ✅ DONE 2026-06-24 — 盘段开/收边界改由 exchange_calendars 取当日真实 UTC session（DST+半日自适应）；实测夏 20:00/冬 21:00 UTC 收盘正确。 |
| B13 | ~~llm.py 未设 temperature~~ ✅ DONE 2026-06-24 — llm.run 增 temperature 参数，默认 0.3（可复现且不过度死板）。 |
| B14 | ~~盘中事件警报~~ ✅ DONE 2026-06-24 — cockpit/intraday_alert.py：破位/日内±6%异动/当日新闻触发，每30min美股时段(GHA)，仅触发才发邮件，(票·条件·日)去重，代码直出无LLM；新增 intraday-alert.yml + config.alerts。（⏸ 2026-07-19 退役：workflow Disabled，理由见 B30） |
| B15 | ~~headroom token 压缩~~ ❌ WON'T-DO 2026-06-24（用户决定移除）——[:90000]/[:95000] 截断够用，候选表已代码直出不靠 token 预算。 |
| B16 | ~~并排对比原版 serenity~~ ❌ WON'T-DO 2026-06-24（用户决定移除）——系统已远超原版样板，无需回头对比。 |
| B17 | ~~SEC EDGAR 深度交叉验证~~ ✅ DONE 2026-06-24 — crossval.edgar_dossier：真实流通股YoY(拆股不误报)+近180天增发类备案(S-3/424B5/FWP,以流通股为准防发债误报)+最新关键备案；接入daily_brief风控/待验证段。UA走EDGAR_USER_AGENT/EMAIL_SENDER。 |
| B18 | 邮件 HTML 美化（用户已说稳定后再做）。 |
| B26 | 盘中警报单次运行内不去重：07-10 警报邮件同一条 ROSEN/MSFT 新闻标题出现两次（同标题两条源，(票·条件·日)去重只防跨次运行）。修法：build_alerts 返回前按 (ticker,cond_key) 去重。 ❌ 作废 2026-07-19（警报已退役） |
| B34 | **开仓检测+入场上下文记录（反思发现 2026-07-14）**：现在只有平仓检测（B5），开仓完全无记录——系统永远不知道你「为什么买」。用 last_positions diff 检测新持仓，自动记录入场时的 posture/评分/是否extended/仓位vs上限 到反思记忆，供 B29 记分板和双周复盘对照「入场论点 vs 结果」。你 6-7 月在 extended 状态买 MU/MRVL 这类行为，将来会被自动留痕。 | ✅ DONE 2026-08-22 — 实弹验证：新开仓 RAM 自动留痕（反思记忆 L16：posture=trend-unconfirmed/vs50=-14/off_high=-61） |
| B35 | **QQQ 建仓进度跟踪（反思发现 2026-07-14）**：$100k 嘉信 QQQ 侧（总资产 40%）系统完全不可见——月定投是手动的（嘉信 AIP 不支持 ETF），无人提醒是否执行；回调挂单（−10%/−15% GTC）成交与否无人跟踪。 | ❌ WON'T-DO 2026-08-23（用户明确：嘉信 QQQ 侧全部自管，系统与助手都不需再跟踪/提醒/追问）；B39 行动清单里的 QQQ 定投节奏提示行保留（代码已在，无额外负担） |
| B36 | **子板块聚合敞口警戒（反思发现 2026-07-14）**：相关性兜底只收紧单票上限，不管主题总敞口——当前 memory_hbm 一个主题 MU+SKHYV ≈ 净值 45%+，无任何警报。加规则：单一子板块合计市值 > 净值 X%（建议 40%）→ 风控段+行动清单警示。 | ✅ DONE 2026-08-26 — 代码交付 2026-08-22（`_theme_exposure`：阈值 40%（config theme_exposure_alert_pct 可调）；杠杆按倍数折算——RAM×2 计入 memory_hbm，红线v2⑥落地；daily 行动清单🟣直出 + biweekly 入 bundle）；验收=下封日报出现主题集中警戒行。**实弹验证**：08-25 日报首次出现 🟣 段（memory_hbm $97,225 = 64.4% 净值，警戒线 40%，含 RAM×2 折算）。**曾长期静默且无人察觉**：08-21 与 08-24 两封日报均无 🟣，根因是 B53（SKHY 未归类→落入 `unmapped`，而 `_theme_exposure` 显式豁免 unmapped，系统看到的 memory_hbm 只有 31.3%，真实 62.8%）。教训：**「代码已交付」不等于「功能在跑」——本项从上线到首次真正触发之间隔了 4 天，中间每天都在静默失效。** |
| B37 | **亏损加仓拦截（利弗莫尔铁律机械化，源自潘大PDF 2026-07-18）**：「只在浮盈中加仓，绝不亏损补仓」——B34 开仓检测的延伸：last_positions diff 发现某票股数增加且 unreal_pnl<0 → 次日行动清单顶部大字「⛔ L1 违规：亏损摊平 XX」并自动写入反思记忆。ORCL 摊平/MU 高位加仓这类行为将来次日即被点名。 | ✅ DONE 2026-08-22 — 实弹验证（遗憾地）：SKHY/KLAC/CCXI 三次摊平均被自动点名（反思记忆 L12/L14/L18；08-21 行动清单顶部 ⛔ CCXI） |
| B38 | **回调买点数字化（潘大 20-25% 回调规则 ⊕ Minervini 支撑，2026-07-18）**：radar 的 extended/wait-pullback 定性标签升级为具体等待价：每个候选给「距52周高 −20% = $X / −25% = $Y / 回踩50日线 = $Z」，行动清单据此输出「SNDK 等 $X 下方再评估」，可选提示预挂 GTC limit。 | ✅ DONE 2026-08-22 — 实弹验证：08-21 日报雷达等待价列 + 行动清单等回调候选（PLTR $166.02/$155.64/$137.94 三档具体价） |
| B39 | **大盘择时层·三信号简化版（源自 Asa 回测文 + 潘大总仓位管控，2026-07-18）**：日报宏观段加 估值分位(SPX/NDX PE 历史分位) + VIX 水平/分位 + 距高点回撤档 → 合成规则透明的 0-100 市场位置分。用途①并入 B35：QQQ 月定投节奏提示（正常/高估缓投/深跌加码+动用 D1 回调子弹）；②主动仓热度预算随位置微调。**不引入杠杆ETF（红线）**；Asa 参数不照搬（单资产回测过拟合+合成TQQQ无费税）。 | ✅ DONE 2026-08-22 — v1 实弹验证：市场位置行每日置顶（08-21：81/100 高位/拥挤 + QQQ 定投节奏提示）；CAPE 估值分位=v2 另行排期 |
| B40 | **交互式驾驶舱面板（源自 Sirius 文形态启发，2026-07-18）**：邮件外加一个随点随开的实时面板（Cowork artifact + IBKR 连接器直连）：持仓/风控红绿灯/行动清单/市场位置，供决策现场用；不动 GitHub repo。邮件=推送纪律，面板=盘中工具。 | ✅ DONE 2026-08-22 — 用户确认桌面重装后 artifact 仍在侧边栏可用（不入 repo，后续迭代在对话里改） |
| B41 | **B28 扫描器加基本面确认滤网（潘大「全产业链业绩验证」，2026-07-18）**：新高簇行业再核成分股营收/毛利是否同向增长（FMP financial-statement-growth），「价格聚类+业绩验证」双确认，降低纯题材误报。 | ✅ DONE 2026-08-24（随 B28 一并验收）— 已并入 B28 交付 2026-08-22（`_growth_ok`：簇内最强 ≤5 票核 年度营收+毛利双正增长，≥3 家=业绩同向确认；数据缺口如实标注不猜） |
| B30 | **盘中警报重设计（用户 2026-07-11 反馈：现状「没有一点用」）**：07-10 当日 fired 56 条几乎全是新闻标题（截断、无链接、无涨跌上下文、含未持仓的 META、重复 ROSEN），把仅有的两条真信号（ORCL:breach、META:move）淹没。已做减法：config `news_alerts: false`，警报只剩 破位/±6%异动（稀有且可执行，且 B25 后覆盖全部 14 票真实持仓）。后续若重开新闻：需①重大事件关键词过滤(guidance/halt/acquisition/SEC/诉讼判决/盈警) ②每票每日≤3条 ③完整标题+链接 ④破位警报附 现价/止损位/距离% 上下文。 | ❌ 作废 2026-07-19 — **盘中警报整体退役**（用户拍板）：用户时区下（美股盘中=中国深夜）警报信息时效≈0——早上读到时市场已收盘，且当晚日报必然覆盖；夜间保护职责改由 IBKR GTC 止损单承担（用户确认会挂）。workflow 已由用户在 GitHub Disable；intraday_alert.py/yml 保留，未来可一键复用 |
| B31 | **FMP 降级 Starter 兼容（batch→单票 fallback）**：官方对照矩阵已实测核实（登录态浏览器读 DOM）——系统所用端点中唯一 Starter 不可用的是 batch-quote（需 Premium+）；quote / chart-light(5年史,US) / news(史限1年,我们只用3天) / earnings / statement-growth / screener(US,B28可用) / price-target-consensus 在 Starter 全可用。已改 fmp.batch_quote：batch 失败或为空即逐票退回 stable/quote（字段形状已用 live key 验证一致）。价格(年付)：Ultimate $99 → Starter $19 省 $960/年；Premium $49 为零改动中间档。 | ✅ DONE 2026-08-22 — 实弹验证：Starter 降级（2026-07-18 生效）后 07-20→08-21 全部日报无「数据缺口」，batch→单票 fallback 生效 |
| B19 | ~~风控相关性对等集仅 holdings+4 半导体~~ ✅ DONE 2026-06-24 — universe 扩成 holdings+所属子板块全部成分股(只取持仓涉及板块,fan-out 可控)；position_caps 双桶: avg_corr 只对账面持仓、max_corr 扩到同板块成分股,单只拥挤板块票也吃集中度折价；输出加 n_book_peers/n_theme_peers。 |
| B43 | **workflow 无超时护栏（08-06 事故）**：2026-08-06 daily run #46 挂起 15m02s 后被 GHA 平台取消（"All jobs were cancelled"，40 天唯一失败；当日无简报、无 state；根因日志已不可考，疑平台侧或外部 API 挂起）。止血=daily/biweekly yml 加 timeout-minutes（12/15）；自动重试暂不做（GH 失败通知邮件可见性已足够） | 🟡 缓解 2026-08-22 |

## 待你拍板
| D1 | ~~嘉信 QQQ 建仓节奏~~ ✅ 已定 2026-06-27 — 混合：立即30% + 手动月定投30%×6 + 回调子弹40%（−10%≈$674 / −15%≈$636 整数股 GTC）；见 QQQ建仓方案_2026-06.md；执行进度跟踪见 B35 |
| D3 | **NRA 税务口径始终未解（反应式排查发现 2026-08-23）**：所有减仓/处置建议都完全忽略税务，我从 6 月起反复标「待验证」却从未解决。若卖出确有摩擦，三批处置方案的成本结构会变。这不是代码问题，是一个**该一次性查清并写进宪法的事实**（用户身份下的美国资本利得/股息预扣适用条款）。 | ❌ WON'T-DO 2026-08-24 — 用户明确：税务不属于本系统职能范围，不再考虑，也不写进宪法 |
| D2 | ~~RAM 与杠杆红线冲突~~ ✅ 已定 2026-08-22 — **红线 v2（杠杆ETF窄门）用户批准**：仅2x做多；合计≤5%净值；负现金期间不得新开/补仓；硬止损−15%/20日低无条件执行；杠杆ETF亏损加仓=最高级违规；3x/反向/衍生品仍禁；**红线修改仅双周复盘日生效**（程序条款）。RAM 留仓（≈3.4%净值，窄门内合规；按2x基础敞口计入主题集中度=待B36实现）。已写入 config redlines + 宪法 §5/v1.2 |

## 已修（留痕）
IBKR 持仓翻倍(Flex Summary+Lot)、P&L 口径不一致、组合快照列满universe、$30k 硬顶基数、notify 空串崩溃、风控 EWMA+1年相关性+同板块兜底、position_size、财报日历、选股雷达、稀释代理。SKHYV→SKHY 代码再变更（2026-08；FMP 复核 SKHY=SK hynix Inc. ADR/NASDAQ，CIK 2120882，与 SKHYV 同公司）。

## B6 最终核对结果（2026-06-23，对 live IBKR 逐项）
代码全部正确：去重(NVDA50.42/MSFT24.53 与 live 完全一致)、止损位(NVDA$190.09/MSFT$326.18/META$499.20、ORCL already_broken_down)、组合热度1.9%(真实公式)、风控(ORCL超vol上限$5,951→TRIM)、稀释false、平仓检测(今日无)、IBKR-vs-FMP价差已标待验证、9段+检查清单+L1教训。✅ batch-1/2 修复在实盘简报全部生效。
但发现两个**数据时效**问题（非代码 bug）：
| B20 | ~~MV/盈亏用 Flex 旧价~~ ✅ DONE — 市值/盈亏改用 股数×FMP当前价(NVDA→+0.9%)；ibkr_mv 保留为参考。 |
| B21 | **Flex "Last Business Day" 滞后约1天**：简报显示 ORCL 94.78/META 5.34，但 live 已是 ORCL 112.90/META 10.67（你在简报跑完后又买入）。考虑 Flex 周期改 "Today" 或简报明确标注"持仓截至上一交易日收盘"。 | 🟡 缓解 — 已加 as-of 标注+提示改 Flex 周期 Today；股数即时性受 Flex EOD 限制(GHA 无实时) |

## B22 选股雷达不渲染 → 已根治(2026-06-23)
候选/板块表改由**代码直出并强制拼到邮件末尾**（不再交给 LLM，杜绝被 token 预算挤掉）。日报+双周都已接。 ✅ DONE

## 07-10/11 邮件验收纪要（2026-07-11，对 live IBKR 连接器逐项）
本轮验收：daily brief 2026-07-10、盘中警报 2026-07-10、biweekly review 2026-07-11 三封真实邮件。
- 股数/成本与 live 完全一致（NVDA 50.4177/198.34、ORCL 126.2544/198.01、MSFT 32.8368/395.89），去重正常。
- B11/B19 生效：风控表出现 有效相关性(eff_corr) 与 同板块同伴数；NVDA n_theme_peers=7、ORCL/MSFT=2，与 config 子板块成分数学一致。
- B12 生效：18:04 UTC(=14:04 ET) 判 intraday，标注"盘中快照"正确（夏令时口径）。
- B20/B21 生效：快照盈亏用 FMP 现价（NVDA +6.3% 对应 210.745）；顶部有"持仓数据截至 20260709"标注。
- B17 生效：EDGAR 段有真实流通股 YoY（NVDA -0.8% 回购/ORCL +2.6%/MSFT -0.1%）、拆股否、备案清单供人工核。
- B22 生效：雷达表在 daily 与 biweekly 都由代码直出；biweekly LLM 失败时雷达表照发（fail-open 按设计工作）。
- 组合热度 2.0% 公式复算一致（(210.745-191.53)×50.4177+(383.615-316.72)×32.8368)/158,889≈2.0%；ORCL 无有效止损已如实标注低估风险。
- 平仓检测生效：META 已从 live 消失，日报正确标"已平仓"并引用反思记忆。
- 发现并登记：B23（temperature 400 杀掉双周 LLM 段）、B24（风控表用前日市值）、B25（config 持仓漂移，15 vs 4，两票超硬顶无感知）、B26（警报单次运行内重复标题）。
- 本轮未检查：GitHub↔本地除 config.yaml/llm.py 外未逐文件比对（llm.py 已抓 raw 确认一致）；SKHY/CCXI/SPCX 标的身份未验证（待验证）；biweekly 业绩段(NAV vs SPY)因 LLM 失败未能验收，待重跑后补。
- 补充(同日稍后)：又发现 B27（daily state 回写断链，nav 冻结 06-27）；workflow 三份中 intraday 本地=GitHub 一致，biweekly 本地=GitHub 一致，daily 双向漂移已出合并版。SKHY/CCXI/SPCX 身份仍待验证。nav 断档 06-30~07-10 无法自动回填（可选：用 IBKR get_pa_performance 手工补）。
